# `@netly/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @netly/prettier-config
$ npm i -D @netly/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@netly/prettier-config"
}
```
